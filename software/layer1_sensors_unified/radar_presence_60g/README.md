# Layer 1 - 60 GHz Presence Radar

This module provides acquisition and lightweight processing for the Infineon 60 GHz presence radar path.

Main API: `PresenceSource`, `MockPresenceProvider`, and `PresenceProcessor`.
