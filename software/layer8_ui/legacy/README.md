# Layer 8 Legacy

This folder contains the previous Layer 8 implementation focused on sensor process orchestration and preview streams.

Contents include:
- per-sensor run/stop/restart API
- local settings and logs
- static HTML control dashboard
- artifact preview endpoints

Use this as operational fallback/diagnostic tooling while the new Layer 8 backend + frontend stack is adopted.
