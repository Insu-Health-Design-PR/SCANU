"""Layer 5: fuse mmWave JSON / triggers with Layer 4 inference.

Prefer ``from layer5_fusion.capture_fusion import main`` (or ``python -m layer5_fusion.anomaly_report_from_capture``)
to avoid importing heavy dependencies when only the package namespace is needed.
"""
