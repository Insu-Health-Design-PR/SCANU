# Layer 4 — Hugging Face object detection (same stack as Layer 8 thermal inference).
#
# Constraints (per project policy):
#   - No venv: use the same ``python3`` you use for ``uvicorn``.
#   - If PYTHONPATH is set to something that shadows stdlib/site-packages, do not use pip;
#   run installs with:  env -u PYTHONPATH python3 -m pip install --user ...
#
# Jetson Orin / JetPack 6 (L4T R36.x) ships CUDA 12.6 — do not remove apt cuda-toolkit-12-6.
# Install PyTorch wheels built for JP6 + cu126 (user site shadows apt python3-torch 1.8):
#
#   env -u PYTHONPATH python3 -m pip install --user --upgrade --ignore-installed \
#     'torch==2.8.0' 'torchvision==0.23.0' \
#     --index-url https://pypi.jetson-ai-lab.io/jp6/cu126
#
# Note: torch 2.11+ from the same index may require ``libcudss.so.0`` not present on some images;
# stay on 2.8.x unless you install the matching NVIDIA package.
#
# NumPy: torch Jetson wheels often expect NumPy 1.x; if you see ABI errors:
#   env -u PYTHONPATH python3 -m pip install --user 'numpy>=1.26,<2'
#
# Transformers (PyPI):
#   env -u PYTHONPATH python3 -m pip install --user 'transformers>=4.46' accelerate
#
# Video smoke test (from ``software/``):
#   env -u PYTHONPATH python3 -m layer4_inference.examples.run_layer4_video_boxes \
#     --input /path/to/video.mp4 --max-frames 20

transformers>=4.46
safetensors>=0.4.0
accelerate>=0.26
timm>=1.0.0
pillow

# Safe/unsafe CNN training (ImageFolder + ResNet-18): install torchvision to match torch
#   env -u PYTHONPATH python3 -m pip install --user torchvision
#
# YOLO classification (optional): ``pip install ultralytics`` may pull NumPy 2.x, which
# BREAKS many Jetson/NVIDIA PyTorch wheels (built against NumPy 1.x) → "Numpy is not available".
# After installing ultralytics, re-pin NumPy 1.x:
#   env -u PYTHONPATH python3 -m pip install --user 'numpy>=1.26,<2' --force-reinstall
# Or install in one shot:
#   env -u PYTHONPATH python3 -m pip install --user ultralytics 'numpy>=1.26,<2'
#
#   yolo classify train data=<out>/data.yaml model=yolov8n-cls.pt ...
